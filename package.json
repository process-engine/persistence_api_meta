{
  "name": "@process-engine/runtime_layer_meta",
  "version": "0.0.1",
  "description": "Meta setup for all the runtime APIs. Not installable, startable or deployable. Just a wrapper repo for use with meta.",
  "scripts": {
    "build": "meta exec \"npm run build\" --exclude runtime_layer_meta",
    "install": "./reinstall.sh",
    "reinstall": "./reinstall.sh"
  },
  "author": "",
  "license": "ISC"
}
